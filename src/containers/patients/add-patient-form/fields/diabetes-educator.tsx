import { Input } from "components";

export const DiabetesEducator = ({ register }) => {
  return (
    <Input
      label="Diabetes Educator"
      name="DiabetesEducator"
      register={register}
    />
  );
};
