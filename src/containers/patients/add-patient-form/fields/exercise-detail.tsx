import { TextArea } from "components";

export const ExerciseDetail = ({ register }) => {
  return (
    <TextArea
      label="Exercise Detail"
      name="ExerciseDetail"
      register={register}
    />
  );
};
