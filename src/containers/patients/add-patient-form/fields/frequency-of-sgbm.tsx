import { Input } from "components";

export const FrequencyOfSGBM = ({ register }) => {
  return (
    <Input
      label="Frequency Of SBGM"
      name="FrequencyOfSBGM"
      register={register}
    />
  );
};
