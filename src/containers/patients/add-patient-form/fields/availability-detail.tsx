import { TextArea } from "components";

export const AvailabilityDetail = ({ register }) => {
  return (
    <TextArea
      label="Availability Detail"
      name="AvailabilityDetail"
      register={register}
    />
  );
};
